---
# Configuration secret for cred-server
# Replace placeholders with actual values before applying
apiVersion: v1
kind: Secret
metadata:
  name: cred-server-config
  namespace: cred-server
type: Opaque
stringData:
  # Akeyless Access ID for authenticating with gateway
  access-id: "p-qzj686col15oum"
  # Comma-separated list of allowed Akeyless access IDs that can call sync endpoints
  # Leave empty to allow all (demo mode only!)
  allowed-access-ids: ""
